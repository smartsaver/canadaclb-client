import React from 'react'
import Section from '../../../components/Section'

const SmartsaverSection = ({ children }) => {
  return <Section className="SmartsaverSection">{children}</Section>
}

export default SmartsaverSection
